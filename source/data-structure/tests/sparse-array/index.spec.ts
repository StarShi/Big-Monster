import SparseArr from '../../src/sparse-array/index'
// let a = new SparseArr()

test('SparseArr', () => {
    let a = new SparseArr();
    a.parseNomal();
});

// test('SparseArr: 1 + 1 = 2',()=>{
//     expect(SparseArr(1,1)).toBe(2);
//  });